/**
 * COOP module — society farmers, milk passbook, input orders (ERP mirror).
 * ← THE WEDGE (blueprint §7). Phase 0 ships the data model + ERP sync backbone;
 * the passbook UX, 70% order-limit engine and demand windows land in Phase 1.
 *
 * Routes/controllers/services are intentionally empty in Phase 0 — the app
 * authors only SUBMITTED★ and RECEIPT_CONFIRMED★ transitions, built in Phase 1.
 */
module.exports = {};
